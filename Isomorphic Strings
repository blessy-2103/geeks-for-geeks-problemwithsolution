class Solution {
    public boolean isIsomorphic(String s, String t) {
        for(int i = 0;i<s.length();i++){
            char ch = s.charAt(i);
            char c = t.charAt(i);
            if(s.indexOf(ch) != t.indexOf(c)){
                return false;
            }
        }
        return true;
    }
}
